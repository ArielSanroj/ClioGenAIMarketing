I see the issue. In the analyzer.py, when clicking "Continue to Chat", the page navigation isn't working correctly. Here's how to fix it:

First, make sure the button logic in display_analysis_results function in analyzer.py is properly set up:
def display_analysis_results(result: Optional[CompanyInfo]):
    """Display the analysis results in an organized layout"""
    # ... existing display code ...
    # Continue Button - Show for both file and website analysis
    if st.session_state.analysis_complete:
        st.markdown("---")
        # Use columns to center the button
        _, col2, _ = st.columns([1, 2, 1])
        with col2:
            if st.button("Continue to Chat", type="primary", use_container_width=True):
                st.session_state.show_chat = True
                st.session_state.current_page = 'chat'
                # Important: Use the correct path to main.py
                st.switch_page("pages/main.py")  # Adjust this path based on your project structure
Make sure your project structure follows Streamlit's multi-page app convention:
your_project/
├── analyzer.py          # This should be in pages/
├── pages/              # Create this directory if it doesn't exist
│   ├── main.py        # Move main.py here if it's not already
│   └── other_pages.py
└── requirements.txt
Update the imports in main.py to handle the navigation:
import streamlit as st
from components.analyzer import render_analyzer
from components.content_generator import render_content_generator
# ... other imports ...
# Check if we're coming from analyzer
if st.session_state.get('show_chat', False) and st.session_state.get('current_page') == 'chat':
    # Show the chat interface
    if st.session_state.get('analyzed_data'):
        # Display analyzed data and chat interface
        st.success("Analysis completed! Let's start chatting about your website.")
        # ... rest of your chat interface code ...
Add a proper initialization in analyzer.py:
def initialize_session_state():
    """Initialize session state variables"""
    if 'analyzed_data' not in st.session_state:
        st.session_state.analyzed_data = None
    if 'show_chat' not in st.session_state:
        st.session_state.show_chat = False
    if 'analysis_complete' not in st.session_state:
        st.session_state.analysis_complete = False
    if 'current_page' not in st.session_state:
        st.session_state.current_page = 'analyzer'
Make sure your main.py is in the correct location and properly handles the incoming navigation:
# main.py
import streamlit as st
# Initialize session state if needed
if 'current_page' not in st.session_state:
    st.session_state.current_page = 'main'
# Check if we're coming from analyzer
if st.session_state.get('show_chat', False) and st.session_state.get('current_page') == 'chat':
    # Display the chat interface
    st.write("Welcome to the chat interface!")
    # ... your chat interface code ...
else:
    # Display your regular main page content
    st.title("Main Page")
    # ... rest of your main page code ...
The key changes are:

Using the correct path in st.switch_page()
Properly setting up the project structure following Streamlit's conventions
Ensuring proper session state management across pages
Handling the navigation state in main.py
Make sure your project structure follows Streamlit's multi-page app convention and that the paths in st.switch_page() are correct for your specific setup.